$(document).ready(function(){
    //Anything inside the curly brackets after this function will only run after the entire dom is loaded
    //$("element").addClass("ClassNameWithNoDot") use this to style rather than .css as this method doesn't use inline styling
    //$("element").removeClass("ClassNameWithoutDot") removes a class from specified element
    $("h2").css("text-decoration")
});